(ns hgp.cljito.hygienic.macro-sys
  (:require  [clojure.walk :refer :all]
             [clojure.pprint :refer :all]
             [hgp.cljito.real-fun-checkers :refer :all]))

(defn- convert-macro-spec-chars [input]
  )

(defn- parse-macro-spec-chars [input]
  )

(defmacro syntax-object [obj])
(defmacro syntax-case [stx-obj])
(defmacro define-syntax [id stx]

  )